/** @type {import('next').NextConfig} */
const nextConfig = {
  // Add your external image host(s) here to use next/image, e.g.:
  // images: { remotePatterns: [{ protocol: 'https', hostname: 'your-cdn.com' }] },
};

module.exports = nextConfig;
